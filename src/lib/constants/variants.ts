/**
 * Quiz variant configuration constants
 */

/** Maximum number of correct answers for simple guess (unlimited in practice, but capped for sanity) */
export const MAX_SIMPLE_GUESS_ANSWERS = 10;

/** Maximum number of options for multiple choice */
export const MAX_MULTIPLE_CHOICE_OPTIONS = 10;

/** Maximum number of options for multiple response */
export const MAX_MULTIPLE_RESPONSE_OPTIONS = 10;

/** Maximum number of tracks for audio sequence */
export const MAX_SEQUENCE_TRACKS = 10;

/** Maximum number of items for audio ranking */
export const MAX_RANK_ITEMS = 10;

/** Maximum number of image options for image choice */
export const MAX_IMAGE_CHOICE_OPTIONS = 10;

/** Minimum number of correct answers for simple guess */
export const MIN_SIMPLE_GUESS_ANSWERS = 1;

/** Minimum number of options for multiple choice */
export const MIN_MULTIPLE_CHOICE_OPTIONS = 2;

/** Minimum number of options for multiple response */
export const MIN_MULTIPLE_RESPONSE_OPTIONS = 2;

/** Minimum number of tracks for audio sequence */
export const MIN_SEQUENCE_TRACKS = 2;

/** Minimum number of items for audio ranking */
export const MIN_RANK_ITEMS = 2;

/** Minimum number of image options for image choice */
export const MIN_IMAGE_CHOICE_OPTIONS = 2;
