/**
 * Quiz variant configuration constants
 */

/** Maximum number of items per variant (tracks, options, images, etc.) */
export const MAX_ITEMS_PER_VARIANT = 10;

/** Minimum number of items required for rank variants */
export const MIN_ITEMS_FOR_RANK = 2;
